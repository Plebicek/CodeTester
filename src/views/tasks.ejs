<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Topic <%= topic %></h1>
        <h3>Tasks</h3>
      <ul>
        <% if (tasks.length > 0) { %>
            <% tasks.forEach(task => { %>
                <li><a href="<%= path %>/task/<%= task.task_id %>"><%= task.task_title %></a></li>
            <% }) %> 
        <% } else { %>
            <li>No tasks assigned</li>
        <% } %>
    </ul>
</body>
</html>