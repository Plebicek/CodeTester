<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <h1 class="text-3xl mx-auto">Grades</h1>
    <ul>
        <% grades.forEach(function(grade) { %>
             <li><a href="/grade/<%= grade.grades.grade_id %>">Ročník <%= grade.grades.grade_name %></a></li>
        <% }); %>
    </ul>
</body>
</html>